name: gestion_stock_vente
description: Gestion de stock et vente (HTG) - mobile
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.2.0
  path_provider: ^2.0.12
  path: ^1.8.2
  image_picker: ^0.8.7+5
  flutter_local_notifications: ^13.0.0
  mailer: ^6.2.7
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/